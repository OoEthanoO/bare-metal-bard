/** @type {import('next').NextConfig} */

// Deployed on Vercel, which builds and serves Next.js natively. That removes
// the two things a GitHub Pages deploy needed: a static `output: 'export'`, and
// a basePath — Pages serves project sites from /<repo>, Vercel serves from the
// domain root.
const nextConfig = {};

export default nextConfig;
